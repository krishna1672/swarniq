/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Add remote image hosts here once product/hero assets live on a CDN.
    remotePatterns: [],
  },
};

export default nextConfig;
